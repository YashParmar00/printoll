/**
 * Hero 3D asset URLs, kept free of three.js so the hero can start preloading
 * them before the WebGL chunk has downloaded.
 *
 * Model: MIT, see public/uploads/hero/tshirt.LICENSE.txt. Textures downsized to
 * 512px (the tee never renders larger than ~600 CSS px) and meshopt-compressed.
 */
export const TEE_MODEL_SRC = "/uploads/hero/versioned/tshirt.5c92abbf263553c2.glb";
